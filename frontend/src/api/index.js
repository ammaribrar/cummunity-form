import auth from './auth';
import posts from './posts';
import comments from './comments';
import users from './users';

export { auth, posts, comments, users };

export default {
  auth,
  posts,
  comments,
  users
};
